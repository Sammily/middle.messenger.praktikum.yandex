.profile {
    align-items: center;
}

.profile__inputs-container {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    pointer-events: none;
}

.profile__label {
    text-align: right;
    clear: both;
    float: left;
    margin-right: 15px;
}